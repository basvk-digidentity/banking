<!DOCTYPE html>
<html>
  <head>
    <title>Banking</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
    <% if logged_in? %>
    <div style="margin-top: 3rem"><%= button_to('Log out', logout_path, method: :delete) %></div>
    <% end %>
  </body>
</html>
